const userModel=require('../models/user.model');
import userService from '../service/user.service';
import validationFunction from './validateFunction'
export const registerUser=async(req,res,next)=>{
    try {
         
    } catch (error) {
        
    }
}

module.exports={
    registerUser
}